{
    "name": "canon-keeper-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "Top-level build orchestration for Canon Keeper",
    "scripts": {
      "build:client": "npm run build --prefix client",
      "build:server": "python server/build_server.py",
      "build": "echo \"--- Building Client --- \" && npm run build:client && echo \"--- Building Server --- \" && npm run build:server",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "workspaces": [
      "client",
      "server"
    ]
  }